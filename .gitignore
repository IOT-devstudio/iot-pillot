# Node / pnpm
node_modules/
.pnpm-store/
dist/
build/
*.local
*.tsbuildinfo

# Vite / unplugin / vue-tsc generated
apps/web/auto-imports.d.ts
apps/web/components.d.ts
apps/web/vite.config.d.ts
apps/web/vite.config.js

# Go
bin/
*.exe
*.dll
*.so
*.dylib
*.test
*.out
coverage.out
coverage.html

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Env
.env
.env.*
!.env.example

# 本地真实配置（含数据库 / SMTP 凭据，绝不入库）
# config.example.yaml 是模板，必须保持被追踪
apps/api/config.yaml
apps/api/config.yml
apps/api/configs/*.yaml
!apps/api/configs/config.example.yaml

# 测试文件：跟着源码进版本库（项目历史一致——PR #33/#43/#60/#62 都含 .test.ts）。
# 文件留在工作区，go test / vitest 照常能跑。
# 之前 `*.test.ts` / `*.spec.ts` / `*_test.go` 三条忽略会让新建的测试文件
# 默认被忽略，与项目实际做法冲突；移除即可，需要时单独再 `git add -f`。
#（贡献者 PR 添加测试时若遇到此提示，请用 `git add -f <文件>` 显式加入。）

# Build artifacts / caches
coverage/
.cache/
.turbo/
.eslintcache

# Generated Go binaries
apps/api/api
temp/

# Local agent notes
AGENTS.md
# 本地视觉工具（截图/取色等）在仓库根留下的产物目录
.dsh-vision-router/
