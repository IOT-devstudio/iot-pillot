# Node / pnpm
node_modules/
.pnpm-store/
dist/
build/
*.local
*.tsbuildinfo

# Vite / unplugin / vue-tsc generated
apps/web/auto-imports.d.ts
apps/web/components.d.ts
apps/web/vite.config.d.ts
apps/web/vite.config.js

# Go
bin/
*.exe
*.dll
*.so
*.dylib
*.test
*.out
coverage.out
coverage.html

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Env
.env
.env.*
!.env.example

# 本地真实配置（含数据库 / SMTP 凭据，绝不入库）
# config.example.yaml 是模板，必须保持被追踪
apps/api/config.yaml
apps/api/config.yml
apps/api/configs/*.yaml
!apps/api/configs/config.example.yaml

# 测试文件：只在本地跑，不进版本库
# 注意：文件仍然留在工作区，go test / vitest 照常能跑；
# 这里只是让它们不被 git add 带走（含 git add . 的批量操作）。
*_test.go
*.test.ts
*.spec.ts

# Build artifacts / caches
coverage/
.cache/
.turbo/
.eslintcache

# Generated Go binaries
apps/api/api
temp/

# Local agent notes
AGENTS.md

# 本地视觉工具（截图/取色等）在仓库根留下的产物目录
.dsh-vision-router/

# Local dev helpers / secrets
dev-jwt-secret.txt
start-backend.ps1
/NUL
