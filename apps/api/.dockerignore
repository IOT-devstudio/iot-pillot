# Docker build context 排除清单。
#
# Dockerfile 里 `COPY . .` 会把整个 apps/api 塞进镜像层,任何无关文件变动
# 都会 bust 该层缓存,导致 go build 白白重跑。这里排掉与构建无关的内容。

# 版本控制与编辑器
.git
.gitignore
.editorconfig
.vscode
.idea
*.swp
*.swo
*~

# 文档
*.md
docs

# 环境与密钥（绝不进镜像）
.env
.env.*

# 构建产物与测试副产物
# 注意：这两条必须用 / 锚定到 context 根目录，裸写 api 容易被误读成会命中
# cmd/api（实际不会，但显式锚定免得下个人删错）。
/api
/bin
*.exe
*.test
*.out
coverage.out
coverage.html

# Docker 自身
Dockerfile
.dockerignore

# OS 垃圾文件
.DS_Store
Thumbs.db
desktop.ini
